---
import Layout from "@layouts/Layout.astro"
import DemoPreviewCard from "@components/DemoPreviewCard.astro"
import { getCollection, type CollectionEntry } from "astro:content"

const allDemos: CollectionEntry<"demos">[] = await getCollection("demos")
---

<Layout title="Demos">
  <section>
    <h2>Demos</h2>
    <p>
      Here you can find all demos on Loopy Demos. Each demo focuses on one pedal
      or piece of gear.
    </p>
  </section>
  <section class="grid-cols-3-min-repeat grid auto-rows-[200px] gap-2">
    {
      allDemos
        .sort((a, b) => b.data.date.getTime() - a.data.date.getTime())
        .map((demo) => (
          <DemoPreviewCard
            slug={demo.slug}
            model={demo.data.model}
            builder={demo.data.builder}
          />
        ))
    }
  </section>
</Layout>
